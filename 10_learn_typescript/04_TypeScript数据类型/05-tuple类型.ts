// tuple 元组 多种元素的组合
// 让拿到的值是指定的类型
const info: [string, number, number] = ['abc', 1, 2]

const arr: [number, boolean] = [1, true]

const name = info[0]
const num1 = info[1]
const num2 = info[2]
console.log(name, num1, num2)

export {}
